import { PolicyContent, policyMetadata } from '@/components/PolicyContent';
export const metadata=policyMetadata('kullanim-kosullari');
export default function Page(){return <PolicyContent slug="kullanim-kosullari"/>}
