import { PolicyContent, policyMetadata } from '@/components/PolicyContent';
export const metadata=policyMetadata('hakkimizda');
export default function Page(){return <PolicyContent slug="hakkimizda"/>}
